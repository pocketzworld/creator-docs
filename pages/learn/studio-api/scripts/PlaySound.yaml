name: PlaySound
type: scripts
summary: |
  PlaySound is a built-in component for playing sound effects in your scene.
  This component can be found in the built-in components section of the Assets Catalog and attached to any GameObject.
  Use it to play sound effects at specific moments or in response to events.
description:
code_samples:
inherits:
tags:
constructors:
properties:
methods:
  - name: Play
    summary: |
      Plays the sound effect associated with this component.
    description:
    is_static: false
    code_samples: |
      ```lua
      --!SerializedField
      local SoundObject : GameObject = nil
      local soundPlayer = SoundObject:GetComponent(PlaySound)
      soundPlayer.Play()
      ```
    tags:
      - ClientOnly
      - NoSelfParameter
    parameters:
    returns:
      - type: void
        summary:
math_operations:
