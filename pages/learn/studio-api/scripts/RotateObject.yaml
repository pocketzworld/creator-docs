name: RotateObject
type: scripts
summary: |
  RotateObject is a built-in component for rotating GameObjects continuously or over a specified duration.
  This component can be found in the built-in components section of the Assets Catalog and dragged onto any GameObject.
  Configure rotation speed, duration, and control playback through pause, resume, and restart methods.
description:
code_samples:
inherits:
tags:
constructors:
properties:
  - name: RotateCompletedEvent
    summary: |
      Event that fires when the rotation animation completes.
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
    type: Class.Event
methods:
  - name: Pause
    summary: |
      Pauses the rotation animation.
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
      - NoSelfParameter
    parameters:
    returns:
      - type: void
        summary:
  - name: Restart
    summary: |
      Restarts the rotation animation from the beginning.
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
      - NoSelfParameter
    parameters:
    returns:
      - type: void
        summary:
  - name: Resume
    summary: |
      Resumes the rotation animation from where it was paused.
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
      - NoSelfParameter
    parameters:
    returns:
      - type: void
        summary:
  - name: SetDuration
    summary: |
      Sets the duration for the rotation animation in seconds.
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
      - NoSelfParameter
    parameters:
      - name: duration
        type: number
        tags:
        default:
        summary:
    returns:
      - type: void
        summary:
  - name: SetRotataionSpeed
    summary: |
      Sets the rotation speed vector for each axis (x, y, z).
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
      - NoSelfParameter
    parameters:
      - name: speedVector
        type: DataType.Vector3
        tags:
        default:
        summary:
    returns:
      - type: void
        summary:
math_operations:
