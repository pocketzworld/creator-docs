name: ScaleObject
type: scripts
summary: |
  ScaleObject is a built-in component for animating the scale of GameObjects over time.
  This component can be found in the built-in components section of the Assets Catalog and dragged onto any GameObject.
  Configure start and end scale values, duration, and control playback through pause, resume, and restart methods.
description:
code_samples:
inherits:
tags:
constructors:
properties:
  - name: ScaleCompletedEvent
    summary: |
      Event that fires when the scale animation completes.
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
    type: Class.Event
methods:
  - name: Pause
    summary: |
      Pauses the scale animation.
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
      - NoSelfParameter
    parameters:
    returns:
      - type: void
        summary:
  - name: Restart
    summary: |
      Restarts the scale animation from the beginning.
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
      - NoSelfParameter
    parameters:
    returns:
      - type: void
        summary:
  - name: Resume
    summary: |
      Resumes the scale animation from where it was paused.
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
      - NoSelfParameter
    parameters:
    returns:
      - type: void
        summary:
  - name: SetDuration
    summary: |
      Sets the duration for the scale animation in seconds.
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
      - NoSelfParameter
    parameters:
      - name: duration
        type: number
        tags:
        default:
        summary:
    returns:
      - type: void
        summary:
  - name: SetEndScale
    summary: |
      Sets the target end scale for the animation.
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
      - NoSelfParameter
    parameters:
      - name: endScale
        type: DataType.Vector3
        tags:
        default:
        summary:
    returns:
      - type: void
        summary:
  - name: SetStartScale
    summary: |
      Sets the starting scale for the animation.
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
      - NoSelfParameter
    parameters:
      - name: startScale
        type: DataType.Vector3
        tags:
        default:
        summary:
    returns:
      - type: void
        summary:
math_operations:
