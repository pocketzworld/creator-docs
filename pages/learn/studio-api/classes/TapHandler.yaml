name: TapHandler
type: classes
summary: |
  A TapHandler is a class that can be used to detect when a GameObject has been tapped.
  In order for a GameObject to be tappable, it must also have a Collider component attached to the same GameObject as the TapHandler or to a parent GameObject.
description:
code_samples:
inherits:
  - Class.WorldBehaviour
tags:
constructors:
properties:
  - name: Tapped
    summary: |
      Event fired when the tap handler is activated. Connect to this event to add custom behavior when the tap handler is tapped.
      Event Parameters:
          Position: the position on the TapHandler's collider that was tapped.
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
      - ReadOnly
    type: Class.Event
  - name: distance
    summary: |
      If CheckDistance is enabled, the character must be within this distance of the tap handler when it is tapped for it to activate.  If move to is enabled, if the character is too far away, they will move within this distance of the TapHandler and then activate it.
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
    type: number
  - name: hasAnchors
    summary: |
      Returns true if the tap handler has anchors
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
      - ReadOnly
    type: boolean
  - name: moveTarget
    summary: |
      If the character has to move to activate the move handler, this is the position the character will move to.  This is the position of the move target if it exists, otherwise it is the tap handler's position.
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
      - ReadOnly
    type: DataType.Vector3
  - name: moveTo
    summary: |
      If CheckDistance is enabled, when a tap handler is tapped and the character is outside the tap handler's distance, the character will move to the TapHandler and activate it if MoveTo is true. If MoveTo is false, nothing happens.
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
    type: boolean
methods:
  - name: GetClosestAnchor
    summary: |
      Returns the closest anchor to the given world position
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
    parameters:
      - name: position
        type: DataType.Vector3
        tags:
        default:
        summary: |
          The position to check for the nearest anchor.
    returns:
      - type: Class.Anchor
        summary: |
          The closest anchor associated with this TapHandler, if it exists.
  - name: Perform
    summary: |
      Activates the TapHandler as if it had been tapped.
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
    parameters:
      - name: position
        type: DataType.Vector3
        tags:
        default:
        summary: |
          The position on the tap handler's collider that was tapped.
    returns:
      - type: void
        summary:
math_operations:
