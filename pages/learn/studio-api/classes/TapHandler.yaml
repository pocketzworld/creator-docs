name: TapHandler
type: classes
summary: |

description:
code_samples:
inherits:
  - Class.Behaviour
constructors:
properties:
  - name: Tapped
    summary: |

    description:
    is_static: false
    code_samples: |
      ```lua
      self.gameObject:GetComponent(TapHandler).Tapped:Connect(function()
          print("Tapped")
      end)
      ```
    type: Class.Event
  - name: MoveTo
    summary: |

    description:
    is_static: false
    code_samples:
    type: boolean
  - name: Distance
    summary: |

    description:
    is_static: false
    code_samples:
    type: number
methods:
  - name: Perform
    summary: |

    description:
    is_static: false
    code_samples:
    parameters:
    returns:
      - type: void
        summary:
math_operations:
