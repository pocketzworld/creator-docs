name: SingleClickEvent
type: classes
summary: |
  Event sent when a single click occurs on an element that tracks double clicks
  That is when the double click timeout is reached without the second click
description:
code_samples:
inherits:
  - Class.EventBase
tags:
constructors:
properties:
  - name: position
    summary:
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
    type: DataType.Vector2
  - name: eventTypeId
    summary:
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
      - ReadOnly
    type: number
methods:
math_operations:
