name: AudioClip
type: classes
summary: |
  AudioClip serves as a container for audio data, including music, voiceovers, and sound effects. It offers properties and methods to access playback details like length, samples, channels, and frequency. AudioClip supports preloading, 3D audio, and background loading. Custom AudioClip instances can be created programmatically. Fundamental for auditory experiences in Unity projects, AudioClip documentation provides further details.
  [Unity's AudioClip Documentation](https://docs.unity3d.com/2022.3/Documentation/ScriptReference/AudioClip.html)
description:
code_samples:
inherits:
  - Class.AudioResource
tags:
constructors:
properties:
  - name: channels
    summary: |
      Represents the number of audio channels, such as 1 for mono or 2 for stereo.
      [Unity's AudioClip.channels Documentation](https://docs.unity3d.com/2022.3/Documentation/ScriptReference/AudioClip-channels.html)
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
      - ReadOnly
    type: number
  - name: frequency
    summary: |
      Denotes the sample rate of the clip in kHz, indicating the samples per second.
      [Unity's AudioClip.frequency Documentation](https://docs.unity3d.com/2022.3/Documentation/ScriptReference/AudioClip-frequency.html)
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
      - ReadOnly
    type: number
  - name: length
    summary: |
      Returns the clip's duration in seconds, akin to a song's length.
      [Unity's AudioClip.length Documentation](https://docs.unity3d.com/2022.3/Documentation/ScriptReference/AudioClip-length.html)
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
      - ReadOnly
    type: number
  - name: loadInBackground
    summary: |
      Specifies if the clip should load in the background, allowing gameplay to continue.
      [Unity's AudioClip.loadInBackground Documentation](https://docs.unity3d.com/2022.3/Documentation/ScriptReference/AudioClip-loadInBackground.html)
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
      - ReadOnly
    type: boolean
  - name: preloadAudioData
    summary: |
      Controls whether the audio data should be preloaded upon clip asset loading.
      [Unity's AudioClip.preloadAudioData Documentation](https://docs.unity3d.com/2022.3/Documentation/ScriptReference/AudioClip-preloadAudioData.html)
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
      - ReadOnly
    type: boolean
  - name: samples
    summary: |
      Refers to the total number of audio samples within the clip.
      [Unity's AudioClip.samples Documentation](https://docs.unity3d.com/2022.3/Documentation/ScriptReference/AudioClip-samples.html)
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
      - ReadOnly
    type: number
methods:
  - name: LoadAudioData
    summary: |
      Initiates loading of the clip's audio data, particularly when 'preloadAudioData' is false.
      [Unity's AudioClip.LoadAudioData Documentation](https://docs.unity3d.com/2022.3/Documentation/ScriptReference/AudioClip.LoadAudioData.html)
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
    parameters:
    returns:
      - type: boolean
        summary: |
          True if data loading starts successfully.
  - name: UnloadAudioData
    summary: |
      Unloads the clip's audio data from memory, aiding in efficient memory usage.
      [Unity's AudioClip.UnloadAudioData Documentation](https://docs.unity3d.com/2022.3/Documentation/ScriptReference/AudioClip.UnloadAudioData.html)
    description:
    is_static: false
    code_samples:
    tags:
      - ClientOnly
    parameters:
    returns:
      - type: boolean
        summary: |
          True if data unloading begins successfully.
math_operations:
