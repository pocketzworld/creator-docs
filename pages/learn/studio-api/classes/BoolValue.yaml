name: BoolValue
type: classes
summary: |
  BoolValue class stores a single boolean value, extending from Class.Value. It's designed for synchronizing a boolean value across all clients in a networked environment, ensuring consistency of a specific flag or state, such as a game over condition. To effectively utilize BoolValue, familiarity with the base Class.Value documentation is advised.
description:
code_samples:
inherits:
  - Class.NetworkValue
tags:
constructors:
  - name: new
    summary: |
      Creates a new BoolValue instance with a specified name and initial value, used for unique identification and initial state setting across clients.
    description:
    is_static: true
    code_samples:
    tags:
      - ClientAndServer
    parameters:
      - name: name
        type: string
        tags:
        default:
        summary:
    returns:
      - type: Class.BoolValue
        summary:
  - name: new
    summary: |
      Creates a new BoolValue instance with a specified name and initial value, used for unique identification and initial state setting across clients.
    description:
    is_static: true
    code_samples:
    tags:
      - ClientAndServer
    parameters:
      - name: name
        type: string
        tags:
        default:
        summary:
      - name: initialValue
        type: boolean
        tags:
        default:
        summary:
    returns:
      - type: Class.BoolValue
        summary:
  - name: new
    summary: |
      Creates a new BoolValue instance with a specified name and initial value, used for unique identification and initial state setting across clients.
    description:
    is_static: true
    code_samples:
    tags:
      - ClientAndServer
    parameters:
      - name: name
        type: string
        tags:
        default:
        summary:
      - name: initialValue
        type: boolean
        tags:
        default:
        summary:
      - name: player
        type: Class.Player
        tags:
        default:
        summary:
    returns:
      - type: Class.BoolValue
        summary:
properties:
  - name: value
    summary: |
      Represents the synchronized boolean value. Changes trigger notifications across clients via the Value.Changed event.
    description:
    is_static: false
    code_samples:
    tags:
      - ClientAndServer
    type: boolean
methods:
math_operations:
