name: WalletError
type: enums
summary: |
  Error codes returned by wallet operations (GetWallet, TransferGold).
  Check for `WalletError.None` to confirm successful transfers.
  
  Available on: Server scripts only
description:
code_samples: |
  ```lua
  Wallet.TransferGoldToPlayer(player, 100, function(walletState, error)
      if error == WalletError.None then
          print("Transfer successful!")
      elseif error == WalletError.InsufficientResources then
          print("Not enough gold in wallet")
      elseif error == WalletError.UserNotFound then
          print("User not found")
      elseif error == WalletError.RequestThrottled then
          print("Too many requests, wait before retrying")
      else
          print("Transfer failed: " .. tostring(error))
      end
  end)
  ```
inherits:
tags:
constructors:
properties:
  - name: InsufficientResources
    summary: |
      The world wallet does not have enough gold to complete the transfer.
      Check wallet balance with GetWallet() before attempting large transfers.
      World creators need to top up their wallet to continue rewarding players.
    description:
    is_static: true
    code_samples:
    tags:
      - ServerOnly
      - ReadOnly
    type: Enum.WalletError
  - name: InternalError
    summary: |
      An internal server error occurred. Retry the operation.
    description:
    is_static: true
    code_samples:
    tags:
      - ServerOnly
      - ReadOnly
    type: Enum.WalletError
  - name: None
    summary: |
      Operation completed successfully with no errors.
    description:
    is_static: true
    code_samples:
    tags:
      - ServerOnly
      - ReadOnly
    type: Enum.WalletError
  - name: RequestThrottled
    summary: |
      Too many wallet requests sent too quickly. Wait before retrying.
      Rate limiting is in effect to prevent abuse.
    description:
    is_static: true
    code_samples:
    tags:
      - ServerOnly
      - ReadOnly
    type: Enum.WalletError
  - name: UserNotFound
    summary: |
      The specified user ID does not exist or is invalid.
      Verify the user ID is correct before retrying.
    description:
    is_static: true
    code_samples:
    tags:
      - ServerOnly
      - ReadOnly
    type: Enum.WalletError
methods:
math_operations:
