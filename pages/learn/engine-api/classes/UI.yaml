name: UI
type: classes
summary: |
  Accessor to the Highrise UI system.
description:
code_samples:
inherits: 
constructors: 
properties: 
methods:
  - name: OpenPopup
    summary: |
      Opens the provided UIView as a popup.
    description: |
      This method is used to open a popup window of the given UIView in Highrise.
    is_static: true
    code_samples:
    parameters:
      - name: ve
        type: Class.UIView
        default:
        summary: The UIView content of the popup.
    returns:
      - type: number
        summary: The unique identifier for the opened popup.
math_operations: 