name: Avatar
type: classes
summary: |
  The Avatar class is a special subtype of the Character within the Highrise game world. An Avatar represents a player-controlled character in the game, with additional properties and methods that give it more control and interaction possibilities, distinguishing it from other types of Characters.
inherits:
  - Class.Character
constructors:
properties:
  - name: Name
    summary: This property assigns a unique name identifier to each Avatar.
    description:
    is_static: false
    code_samples:
    type: string
  - name: IsLoaded
    summary: This property confirms when all the assets related to the Avatar have been successfully loaded into the game.
    description:
    is_static: false
    code_samples:
    type: boolean
  - name: PetIsLoaded
    summary: Similar to IsLoaded but checks if the assets related to the Avatar's pet have loaded successfully.
    description:
    is_static: false
    code_samples:
    type: boolean
  - name: IsSpeaker
    summary: This property indicates if the Avatar's owner is marked as a speaker in the room.
    description:
    is_static: false
    code_samples:
    type: boolean
  - name: IsSpeaking
    summary: This property checks to see if the Avatar's user is currently speaking.
    description:
    is_static: false
    code_samples:
    type: boolean
  - name: IsMuted
    summary: This property allows you to check or set if the Avatar has been muted, meaning its user cannot speak with other.
    description:
    is_static: false
    code_samples:
    type: boolean
  - name: IsDeafened
    summary: This property verifies if the Avatar has been deafened, which means that its user cannot hear others speaking.
    description:
    is_static: false
    code_samples:
    type: boolean
  - name: Height
    summary: This property refers to the Avatar's height.
    description:
    is_static: false
    code_samples:
    type: number
methods:
  - name: MoveTo
    summary: This method moves an Avatar to a specific location in the game's local environment.
    description:
    is_static: false
    code_samples:
    parameters:
      - name: position
        type: DataType.Vector3
        default:
        summary: The target location to move the Avatar to.
    returns:
      - type: void
        summary:
math_operations: 