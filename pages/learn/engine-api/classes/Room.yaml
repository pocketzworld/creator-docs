name: Room
type: class
summary: |
  The object that contains Entities in a Highrise world.
description: |
  The object that contains Entities in a Highrise world.
code_samples:
inherits:
properties:
  - name: ambientColor
    summary:
    description:
    code_samples:
    type: color
  - name: backgroundColor1
    summary:
    description:
    code_samples:
    type: color
  - name: backgroundColor2
    summary:
    description:
    code_samples:
    type: color
  - name: localPlayer
    summary: |
      The curring user's local avatar.
    description:
    code_samples:
    type: Avatar
  - name: sunColor
    summary:
    description:
    code_samples:
    type: color
methods:
  - name: createEntity
    summary: |
      Creates a new entity.
    description:
    code_samples:
    parameters:
      - name: asset
        type: prefabAsset
        default:
        summary: |
          (Optional) The asset to create an entity copy from. Default is an empty entity.
      - name: name
        type: Entity
        default: 
        summary: |
          (Optional) The parent entity to attach this creation to. Default is the room itself.
    returns:
      - type: Entity
        summary: |
          The created entity.
  - name: find
    summary: |
      Gets the first entity found with the given name that is in the room.
    description: |
      Returns the first `Class.Entity` found with the given name that is in the room.
      This can be given as a path as well (e.g. "Child/Grandchild").
    code_samples:
    parameters:
      - name: self
        type: Room
        default:
        summary: |
          The room to search in.
      - name: name
        type: string
        default:
        summary: |
          The name of the entity to look for.
    returns:
      - type: Entity
        summary: |
          The entity found. This will be nil if none are found.
  - name: findNamed
    summary: |
      Gets all entities with the given name that are in the room.
    description: |
      Returns an array of entities that match the given name that are in the room.
    code_samples:
    parameters:
      - name: self
        type: Room
        default:
        summary: |
          The room to search in.
      - name: name
        type: string
        default:
        summary: |
          The name of the entities to look for.
    returns:
      - type: Array
        summary: |
          The array of entities. This will be empty if none are found.
  - name: findTyped
    summary: |
      Gets entities of given Type that are in the room.
    description: |
      Returns an array of entities that are the given type that are in the room.
    code_samples:
    parameters:
      - name: self
        type: Room
        default:
        summary: |
          The room to search in.
      - name: type
        type: string
        default:
        summary: |
          The name of the type to look for.
    returns:
      - type: Array
        summary: |
          The array of entities. This will be empty if none are found.
  - name: rebuildLighting
    summary: |
      Rebuilds the lighting in the room.
    description: |
      This is useful if you have changed any shadow/color/lighting settings and want to apply the changes.
    code_samples:
    parameters:
      - name: self
        type: Room
        default:
        summary:
    returns:
      - type: void
        summary:
hidden:
  - name: findTagged
    summary: |
      Gets entities with the given tag that are in the room.
    description: |
      Returns an array of entities that have given tag that are in the room.
    code_samples:
    parameters:
      - name: self
        type: Room
        default:
        summary: |
          The room to search in.
      - name: tag
        type: string
        default:
        summary: |
          The name of the tag on the entities to look for.
    returns:
      - type: Array
        summary: |
          The array of entities. This will be empty if none are found.
  - name: focusCameraOn
    summary: |
      Focuses the camera onto the given entity
    description: |
      Returns an array of entities that have given tag that are in the room
    code_samples:
    parameters:
      - name: target
        type: Entity
        default:
        summary: |
          The target entity to focus the camera on
      - name: cameraAngles
        type: Vector3
        default:
        summary: |
          Optional. The Euler angles (degrees) of the camera when focusing on the entity. Default: 0, 28.5, 0
      - name: frameScale
        type: float
        default:
        summary: |
          Optional. The scale (zoom) of the camera when focusing on the entity. Default 1.0
      - name: frameCenter
        type: float
        default:
        summary: |
          Optional. The centering of the camera on the target. Default 0.5
    returns:
      - type: void
        summary: