name: Anchor
type: class
summary: |
  An anchor within the world
description: |
  An `Class.Entity` type that represents an anchor within the world. Anchors are 
  used to position and orient entities within the world.
code_samples:
inherits:
  - Entity
properties:
  - name: entered
    summary: |
      Occurs when an entity enters the anchor.
    description:
    code_samples:
    type: Event
  - name: exited
    summary: |
      Occurs when an entity exits the anchor.
    description:
    code_samples:
    type: Event
methods: