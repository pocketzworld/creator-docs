name: TapHandler
type: classes
summary: |
  A Behaviour that fires a Event when the attached Class.GameObject Collider(s) are tapped.
description:
code_samples:
inherits:
constructors:
properties:
  - name: Tapped
    summary: |
      The Event that is fired when the object is tapped. This requires the Class.GameObject to have a TapHandler attached to either a parent of or the same object as the Collider.
    description:
    is_static: false
    code_samples: |
      #### Handling a Tap
      This small snippet shows how to handle a tap event on the object your script is attached to
      ```lua
      self.gameObject:GetComponent(TapHandler).Tapped:Connect(function()
          print("Tapped")
      end)
      ```
    type: Event
methods:
math_operations:
