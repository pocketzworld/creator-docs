name: TapHandler
type: classes
summary: |
  A Behaviour that can be attached to a GameObject to make them handle tap events.
description:
code_samples:
inherits:
constructors:
properties:
  - name: Tapped
    summary: |
      The LuaEvent that is fired when the object is tapped. This requires the Class.GameObject to have a TapHandler attached to either a parent of or the same object as the Collider.
    description:
    is_static: false
    code_samples: |
      ```lua
      self.gameObject:GetComponent(TapHandler).Tapped:Connect(function()
          print("Tapped")
      end)
      ```
    type: LuaEvent
methods:
math_operations:
