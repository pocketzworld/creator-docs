name: Audio
type: service
summary: |
  `hr.audio` Provides access to audio related functions, such as playing sounds and music.
description:
code_samples:
properties:
methods:
  - name: getMusicIntensity
    summary: |
      Gets the current music intensity.
    description:
    code_samples:
    parameters:
    returns:
      - type: number
        summary:
  - name: play
    summary: |
      Plays a sound.
    description:
    code_samples:
    parameters:
        - name: sound
          type: AudioAsset
          default:
          summary: |
            The audio asset to play.
    returns:
      - type: void
        summary:
  - name: playMusic
    summary: |
      Plays music.
    description:
    code_samples:
    parameters:
      - name: music
        type: AudioAsset
        default:
        summary: |
          The audio asset to play as music.
      - name: volume
        type: number
        default:
        summary: |
          (Optional) The volume to play the music at. Default is max (1.0).
    returns:
      - type: void
        summary:
  - name: playMusic
    summary: |
      Plays music.
    description:
    code_samples:
    parameters:
      - name: url
        type: string
        default:
        summary: |
          The url to fetch music to to play.
      - name: volume
        type: number
        default:
        summary: |
          (Optional) The volume to play the music at. Default is max (1.0).
    returns:
      - type: void
        summary:
  - name: stopMusic
    summary: |
      Stops music.
    description:
    code_samples:
    parameters:
    returns:
      - type: void
        summary: