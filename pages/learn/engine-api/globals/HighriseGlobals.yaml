name: Highrise Globals
type: global
summary: |
  Built-in constants unique to Highrise that can be used in Lua scripts.
description: |
  Highrise Studio has several unique built-in functions, variables, and shortcuts in its Lua
  implementation.
code_samples:
properties:
  - name: entity
    type: Entity
    summary: |
      A reference to the `Class.Entity` this script is attached to.
    description: |
      A reference to the `Class.Entity` this script is attached to.
  - name: room
    type: Room
    summary: |
      The `Class.Room` that this script is executing in.
    description: |
      The `Class.Room` that this script is executing in.
  - name: hr
    type: namespace
    summary: |
      The access point for all Highrise Studio `Services`.
    description:
  - name: script
    type: Script
    summary: |
      A reference to the script object (Script) that is executing this code.
    description: |
      A reference to the script object that is executing the code you are
      writing. Similar to a "this" or "self" reference in other languages.
  - name: transform
    type: Transform
    summary: |
      The `Class.Transform` of the `Class.Entity` this script is attached to.
    description: |
      The `Class.Transform` of the `Class.Entity` this script is attached to.
functions: