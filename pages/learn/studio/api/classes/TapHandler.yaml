name: TapHandler
type: classes
summary: |

description:
code_samples:
inherits:
  - Class.Behaviour
constructors:
properties:
  - name: tapped
    summary: |

    is_static: false
    code_samples: |
      ```lua
      self.gameObject:GetComponent(TapHandler).Tapped:Connect(function()
          print("Tapped")
      end)
      ```
    type: Class.Event
  - name: moveTo
    summary: |

    is_static: false
    code_samples:
    type: boolean
  - name: distance
    summary: |

    is_static: false
    code_samples:
    type: number
methods:
  - name: Perform
    summary: |

    is_static: false
    code_samples:
    parameters:
    returns:
      - type: void
        summary:
math_operations:
