name: ChannelInfo
type: classes
summary: |
  Represents a chat channel in the game that voice and text can be sent through in order to communicate with the correct other players.
description:
code_samples:
inherits:
constructors:
properties:
  - name: players
    summary: |
      The list of players in the channel.
    description:
    is_static: false
    tags:
      - ClientAndServer
    code_samples:
    type: Player[]
  - name: name
    summary: |
      The unique name of the channel.
    description:
    is_static: false
    tags:
      - ClientAndServer
    code_samples:
    type: string
  - name: allowsText
    summary: |
      Whether or not this channel allows text chat.
    description:
    is_static: false
    tags:
      - ClientAndServer
    code_samples:
    type: boolean
  - name: allowsVoice
    summary: |
      Whether or not this channel allows voice chat.
    description:
    is_static: false
    tags:
      - ClientAndServer
    code_samples:
    type: boolean
  - name: isWhisperChannel
    summary: |
      Whether or not this channel is specifically for whispering between participants.
    description:
    is_static: false
    code_samples:
    type: boolean
methods:
math_operations:
