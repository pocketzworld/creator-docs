name: Players
type: service
summary: |
  `hr.players` Provides access to player related functions, such as events when players leave/join.
description:
code_samples:
properties:
  - name: playerLeft
    summary: |
      Event that fires a player leaves the room.
    description:
    code_samples:
    type: Event
  - name: playerJoined
    summary: |
      Event that fires a player joins the room.
    description:
    code_samples:
    type: Event
methods:
  - name: getPlayers
    summary: |
      Gets all players in the room.
    description:
    code_samples:
    parameters:
    returns:
      - type: Array
        summary: |
          An Array of Avatars that represent the players in the room.