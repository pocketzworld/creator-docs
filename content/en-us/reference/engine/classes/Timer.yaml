name: Timer
type: class
summary: |
  A timer that fires events after a given amount of time.
description:
code_samples:
inherits:
properties:
  - name: isPaused
    summary: |
      Whether or not the timer is paused.
    description:
    code_samples:
    type: boolean
  - name: isPlaying
    summary: |
      Whether or not the timer is playing.
    description:
    code_samples:
    type: boolean
methods:
  - name: after
    summary: |
      Creates a timer that fires after a given amount of time.
    description:
    code_samples:
    parameters:
      - name: seconds
        type: number
        default:
        summary: |
          The number of seconds to wait before calling the function.
      - name: closure
        type: function
        default:
        summary: |
          The function to call when the timer fires.
    returns:
      - type: Timer
        summary: |
          The created Timer. Nil if an error occurred in creation.
  - name: every
    summary: |
      Creates a timer that fires every given amount of time.
    description:
    code_samples:
    parameters:
      - name: seconds
        type: number
        default:
        summary: |
          The number of seconds to wait before calling the function.
      - name: closure
        type: function
        default:
        summary: |
          The function to call when the timer fires.
    returns:
      - type: Timer
        summary: |
          The created Timer. Nil if an error occurred in creation.
  - name: pause
    summary: |
      Pauses the timer.
    description:
    code_samples:
    parameters:
      - name: self
        type: Timer
        default:
        summary:
    returns:
      - type: void
        summary:
  - name: play
    summary: |
      Plays the timer.
    description:
    code_samples:
    parameters:
      - name: self
        type: Timer
        default:
        summary:
    returns:
      - type: void
        summary:
  - name: stop
    summary: |
      stops the timer.
    description:
    code_samples:
    parameters:
      - name: self
        type: Timer
        default:
        summary:
    returns:
      - type: void
        summary: